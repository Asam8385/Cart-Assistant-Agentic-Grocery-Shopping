[project]
name = "shoppingagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ASAM", email = "asam.ugeng8385@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "datetime>=6.0",
    "mysql-connector-python>=26.7.0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
shoppingagent = "shoppingagent.main:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
